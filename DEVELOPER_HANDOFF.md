# وثيقة تسليم ومتطلبات منصة TaskFlow

> آخر مراجعة للكود: 29 يوليو 2026  
> الفرع وقت المراجعة: `main`  
> آخر Commit تمت مراجعته: `c2b04cc` — `Add deployable database baseline`

## 1. هدف الوثيقة

هذه الوثيقة هي نقطة البداية للفريق الذي سيستكمل تطوير المنصة. وهي مبنية على فحص الكود الحالي، وليست وصفًا لمنتج مكتمل.

المطلوب من الفريق:

1. تثبيت النسخة الحالية وتشغيلها وفهم حدودها.
2. معالجة مشاكل الجودة والإنتاج الموضحة في الأولويات.
3. استكمال الوظائف الجزئية بعد اعتماد قرارات مالك المنتج.
4. عدم اعتبار وجود Model في Prisma دليلًا على اكتمال واجهته أو رحلة المستخدم الخاصة به.

## 2. تعريف المنتج

`TaskFlow` منصة لإدارة أعمال الفرق والعملاء، مستوحاة من Asana. تدعم حاليًا تنظيم العمل داخل Workspaces، وربط المشاريع بالعملاء، وإدارة المهام الشخصية ومهام المشاريع، وعرضها كقائمة أو Board أو Calendar أو Timeline.

### المستخدمون المستهدفون

- مالك مساحة العمل.
- مدير مساحة العمل.
- عضو فريق.
- ضيف.
- مدير عام للمنصة `Super Admin`.
- عضو مشروع بصلاحية Owner أو Admin أو Editor أو Commenter أو Viewer.
- مراجع جودة لمهمة.

### الكيانات الرئيسية

- User
- Workspace وWorkspaceMember
- Team وTeamMember
- Client
- Project وProjectMember وSection
- Task وSubtask وTaskProjectLink
- Comment وAttachment وNotification وActivityLog
- Quality Review وCriteria وIssues
- Goal وPortfolio
- Custom Fields وTags وDependencies
- Time Entry وBudget وForm وAutomation Rule وProject Template
- Asana Connection وImport Run وExternal Object Map وImport Issue

ملاحظة: بعض الكيانات في القائمة الأخيرة موجودة في قاعدة البيانات فقط أو لا تملك رحلة استخدام كاملة.

## 3. التقنية والبنية الحالية

| الجزء | التقنية الحالية |
|---|---|
| Framework | Next.js `16.2.1` باستخدام App Router |
| UI | React `19.2.4`، Tailwind CSS 4، shadcn/Base UI، Lucide |
| Backend | React Server Components، Server Actions، Route Handlers |
| Auth | NextAuth v4 مع Credentials وJWT sessions |
| ORM | Prisma `5.22` |
| قاعدة التطوير | SQLite |
| السحب والإفلات | `@hello-pangea/dnd` |
| الاختبارات | Node Test Runner مع `tsx` |
| CI | GitHub Actions على Node 20 |

أماكن الكود المهمة:

- الصفحات والـAPIs: `src/app/`
- المكونات التفاعلية: `src/components/`
- عمليات التعديل: `src/actions/`
- الصلاحيات ومنطق الأعمال: `src/lib/`
- قاعدة البيانات: `prisma/schema.prisma`
- Migration الأساسية: `prisma/migrations/20260724000000_init/`
- الاختبارات: `tests/`
- أدوات الاستيراد والصيانة: `scripts/`

تنبيه مهم: هذا المشروع يستخدم Next.js 16 مع تغييرات غير متوافقة مع نسخ سابقة. قبل تعديل APIs أو conventions أو هيكل الملفات، يجب قراءة الدليل المناسب داخل `node_modules/next/dist/docs/` كما يطلب `AGENTS.md`.

## 4. التشغيل المحلي

### المتطلبات

- Node.js 20.
- npm.
- نسخة من ملف `.env.example` باسم `.env`.

### متغيرات البيئة

```env
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"
DATABASE_URL="file:./dev.db"
ASANA_TOKEN_ENCRYPTION_KEY=""
SEED_DEMO_PASSWORD=""
```

- `NEXTAUTH_SECRET` إلزامي في الإنتاج ويجب أن يكون Secret قويًا.
- `DATABASE_URL` يشير حاليًا إلى SQLite.
- `ASANA_TOKEN_ENCRYPTION_KEY` مطلوب قبل تخزين أي Asana token قابل لإعادة الاستخدام.
- `SEED_DEMO_PASSWORD` للتطوير فقط ويجب ألا يستخدم على قاعدة إنتاج.
- يمنع رفع `.env` أو قواعد SQLite أو النسخ الاحتياطية إلى Git.

### أوامر البداية

```bash
npm ci
npm run db:setup
npm run dev
```

اختياريًا، لإنشاء بيانات تجريبية:

```bash
SEED_DEMO_PASSWORD="a-development-only-password"
npm run db:seed
```

لا تشغّل Seed التجريبي على الإنتاج.

## 5. حالة الجودة وقت التسليم

تم تشغيل الفحوص التالية محليًا في 29 يوليو 2026:

| الفحص | النتيجة |
|---|---|
| `npm run db:status` | ناجح؛ Migration واحدة وقاعدة التطوير محدثة |
| `npm run typecheck` | ناجح |
| `npm test` | ناجح؛ 20 من 20 اختبارًا |
| `npm run build` | ناجح؛ Production build اكتمل |
| `npm run lint` | فاشل؛ 130 خطأ و20 تحذيرًا |

أغلب أخطاء lint ناتجة عن `any` غير محدد في المكونات الكبيرة، بالإضافة إلى مشاكل React Effects، و`require()` في Script، وتحذيرات صور ومتغيرات غير مستخدمة.

الـCI الحالي يشغّل:

- بناء Skills manifest.
- TypeScript.
- الاختبارات.
- Production build.

الـCI لا يشغّل `npm run lint` حاليًا؛ لذلك نجاح CI لا يعني أن المشروع اجتاز كل بوابات الجودة.

## 6. المتطلبات الوظيفية وحالة التنفيذ

دليل الحالة:

- **منفذ:** رحلة أساسية موجودة في الواجهة والكود.
- **جزئي:** جزء من الرحلة موجود، لكن توجد وظائف أو حالات ناقصة.
- **غير منفذ:** لا توجد رحلة مستخدم مكتملة، حتى لو كان Model موجودًا.

### 6.1 الحسابات والمصادقة

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| إنشاء حساب بالاسم والبريد وكلمة المرور | منفذ | الحد الأدنى لكلمة المرور 8 أحرف |
| إنشاء Workspace تلقائيًا للمستخدم الجديد | منفذ | المستخدم يصبح Owner ويتم تفعيل الـWorkspace |
| تسجيل الدخول والخروج | منفذ | Credentials + JWT |
| تعديل الاسم والصورة والمنطقة الزمنية | منفذ | يوجد رفع Avatar محلي |
| تغيير كلمة المرور | منفذ | يتطلب كلمة المرور الحالية |
| استعادة كلمة المرور بالبريد | غير منفذ | لا يوجد Forgot/Reset Password |
| تأكيد البريد الإلكتروني | غير منفذ | لا يوجد Email verification |
| MFA/2FA | غير منفذ | |
| إغلاق الحساب أو تصدير بياناته | غير منفذ | |

### 6.2 Workspaces والأعضاء والصلاحيات

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| إنشاء Workspace والتبديل بين Workspaces | منفذ | مع حفظ `active_workspace_id` |
| إعادة تسمية Workspace | منفذ | للـOwner/Admin |
| إضافة عضو وتغيير دوره وحذفه | منفذ جزئيًا | الإضافة الحالية ليست دعوة بريدية كاملة |
| أدوار Workspace | منفذ | Owner، Admin، Member، Guest |
| طلب Super Admin ومراجعته | منفذ | توجد إشعارات للمراجعة |
| إدارة Teams من الواجهة | غير منفذ | Models موجودة لكن لا توجد رحلة إدارة كاملة |

قواعد الصلاحيات الحالية:

- Workspace View: جميع الأدوار.
- Workspace Write: Owner وAdmin وMember.
- Workspace Admin: Owner وAdmin.
- Project View: Owner وAdmin وEditor وCommenter وViewer.
- Project Comment: Owner وAdmin وEditor وCommenter.
- Project Edit: Owner وAdmin وEditor.
- Project Manage: Owner وAdmin.
- المهام الشخصية لا يراها إلا منشئها أو الشخص المعيّن لها.
- مراجع الجودة يحصل على وصول قراءة/تعليق للمهمة المطلوبة للمراجعة، وليس تعديلًا عامًا.

كل عملية Server Action أو API جديدة يجب أن تتحقق من الصلاحية على الخادم، ولا يكفي إخفاء الزر في الواجهة.

### 6.3 العملاء

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| إنشاء عميل وتعديل بياناته | منفذ | الاسم والبريد والملاحظات واللون |
| أرشفة العميل واستعادته | منفذ | |
| حذف العميل | منفذ | يحتاج تأكيد واضح بسبب Cascade |
| عرض مشاريع العميل | منفذ | |
| مهام مباشرة للعميل خارج المشروع | منفذ | |
| البحث داخل أعمال العميل | منفذ | |
| نقل وترتيب المهام بالسحب | منفذ | |
| بوابة مستقلة للعميل الخارجي | غير منفذ | |
| صلاحيات عميل منفصلة عن Workspace | غير منفذ | الوصول الحالي مشتق من Workspace |

### 6.4 المشاريع

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| إنشاء وتعديل مشروع | منفذ | الاسم والوصف والموعد والخصوصية والعرض الافتراضي وغيرها |
| ربط المشروع بعميل | منفذ | |
| أعضاء المشروع وأدوارهم | منفذ | Owner/Admin/Editor/Commenter/Viewer |
| أقسام المشروع | منفذ | إنشاء وحذف وترتيب |
| تمييز المشروع كمفضّل | منفذ | |
| Overview | منفذ جزئيًا | Resource links ما زالت `coming soon` |
| List View | منفذ | إنشاء وتعديل وفتح المهام |
| Board View | منفذ | Kanban وسحب وإفلات |
| Calendar View | منفذ جزئيًا | عرض وتنقل وفتح المهمة، دون تحرير كامل من التقويم |
| Timeline View | منفذ جزئيًا | عرض شهري مبني على التواريخ، وليس Gantt كاملًا |
| Project Dashboard مستقل | غير منفذ | المسار الحالي يعيد التوجيه |
| أرشفة المشروع من الواجهة | يحتاج مراجعة | الحقل موجود، لكن رحلة واضحة مكتملة ليست ظاهرة |
| Templates للمشاريع | غير منفذ | Model فقط |

### 6.5 المهام

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| إنشاء مهمة وتعديلها | منفذ | |
| العنوان والوصف والحالة والأولوية | منفذ | |
| المسؤول وتواريخ البدء والاستحقاق | منفذ | |
| ربط المهمة بعميل أو مشروع أو إبقاؤها شخصية | منفذ | |
| Subtasks | منفذ | إنشاء، إكمال، حذف |
| Comments | منفذ | إضافة وحذف |
| Attachments | جزئي | روابط/Metadata؛ لا يوجد نظام ملفات عام مكتمل |
| عرض النشاط History | منفذ | Activity log |
| My Tasks والأقسام الشخصية | منفذ | مع سحب وإفلات |
| مهام مطلوبة للمراجعة أو إعادة العمل | منفذ | ضمن My Tasks |
| Multi-homing عبر أكثر من مشروع | جزئي | Model وبيانات موجودة، الواجهة تحتاج اختبارًا واستكمالًا |
| Tags | جزئي/غير مكتمل | Models موجودة، ولا توجد إدارة متكاملة واضحة |
| Custom fields | جزئي | الاستيراد يدعمها، والتحرير العام يحتاج مراجعة |
| Dependencies | غير منفذ في الواجهة | Model موجود |
| Time tracking | غير منفذ في الواجهة | Model موجود |
| Forms | غير منفذ | Models فقط |
| Automation rules | غير منفذ | Model فقط |
| التذكيرات المتقدمة والتكرار | غير منفذ | |

### 6.6 دورة مراجعة الجودة

هذه من أكثر الوظائف اكتمالًا في منطق الأعمال:

- Quality policy على مستوى المشروع: Off أو Optional أو Required.
- Reviewer افتراضي وReview SLA بالأيام.
- تعيين Reviewer للمهمة.
- تعريف معايير جودة.
- إرسال المهمة للمراجعة مع ملاحظة.
- قرارات: Approved وApproved with notes وNeeds rework.
- تسجيل Issues بدرجات Minor وMajor وBlocker وأسباب محددة.
- حساب Score وGrade ودورات المراجعة وإعادة العمل.
- إشعارات للمراجع وصاحب المهمة.
- تقارير شهرية تأخذ أول Submission وتستبعد Pending scores حسب الاختبارات.

المطلوب قبل الإنتاج:

- اختبار End-to-End كامل لكل انتقال حالة.
- منع أي انتقال غير مسموح به على الخادم.
- توثيق معنى الدرجات والأسباب للمستخدم.
- التحقق من المناطق الزمنية لمواعيد SLA وRework.

### 6.7 البحث والإشعارات

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| البحث في المشاريع والمهام المتاحة للمستخدم | منفذ | من Topbar |
| Inbox يجمع Notification/Comment/Activity | منفذ | |
| عداد العناصر غير المقروءة | منفذ | |
| تعليم العناصر كمقروءة | منفذ جزئيًا | فتح Inbox يعلّم العناصر الحالية كمقروءة |
| أرشفة الإشعارات | غير منفذ | الواجهة تعرض `Archive soon` |
| Snooze/إدارة التفضيلات | غير منفذ في الواجهة | يوجد حقل `snoozed_until` |
| إشعارات بريد أو Push | غير منفذ | |
| تحديث لحظي Realtime | غير منفذ | |

### 6.8 التقارير والأهداف والمحافظ

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| Reporting عام | منفذ | فلاتر فترة/نطاق/حالة/أولوية |
| مؤشرات Created/Completed/Risk/Quality | منفذ | |
| Breakdown حسب العميل والمشروع والمسؤول | منفذ | |
| تصدير التقرير CSV | منفذ | |
| Goals عرض ومتابعة التقدم | منفذ جزئيًا | Read-only؛ لا توجد CRUD كاملة |
| Portfolios | غير منفذ | المسار يعيد التوجيه إلى Clients |
| Dashboard عام مستقل | غير منفذ | المسار يعيد التوجيه إلى Reporting |
| Budgets وTime Entries في التقارير | جزئي | بياناتها تدخل بعض منطق التقرير، ولا توجد إدارة كاملة |

يوجد خطأ Encoding ظاهر في صفحة Goals (`â€¢`) يجب إصلاحه.

### 6.9 الاستيراد والتصدير وAsana

| المتطلب | الحالة | ملاحظات |
|---|---|---|
| استيراد CSV مع Preview | منفذ | Super Admin فقط |
| الاستيراد إلى مشروع موجود أو جديد أو مهام شخصية | منفذ | |
| Mapping للأعمدة الإضافية إلى Custom Fields | منفذ | |
| إنشاء Placeholder users من البريد غير المعروف | منفذ | يجب اعتماد السياسة أمنيًا |
| تصدير My Tasks إلى CSV | منفذ | |
| تصدير مهام مشروع إلى CSV | منفذ | |
| استيراد Asana CSV package من Script | منفذ | `npm run db:import:asana -- <path>` |
| Asana API client وInventory | موجود في المكتبة | لا توجد رحلة UI مكتملة |
| حفظ اتصال Asana وإدارته من UI | غير منفذ | Models والتشفير موجودان |
| مزامنة ثنائية الاتجاه مع Asana | غير منفذ | |

أي استيراد إنتاجي يجب أن يكون Idempotent أو قابلًا للتراجع، ويسجّل المصدر والنتيجة والأخطاء دون كشف Tokens أو بيانات حساسة.

## 7. الأولويات المقترحة للفريق

### P0 — قبل أي إطلاق إنتاجي

1. إصلاح جميع أخطاء lint وإضافة `npm run lint` إلى CI.
2. إضافة اختبارات Integration وEnd-to-End للـAuth والصلاحيات والمهام والاستيراد والجودة.
3. اتخاذ قرار قاعدة الإنتاج ثم تنفيذ Migration من SQLite إلى PostgreSQL أو قاعدة مدارة مناسبة.
4. إعداد تخزين دائم وآمن للصور والملفات؛ التخزين المحلي لا يناسب بيئة Serverless أو أكثر من Instance.
5. مراجعة أمنية لمسارات التسجيل، إضافة الأعضاء، رفع الصور، الاستيراد، والتعامل مع الروابط.
6. إضافة Rate limiting على login/register/import/upload والعمليات الحساسة.
7. إضافة Password reset وEmail verification أو تعطيل التسجيل العام إذا كانت المنصة داخلية.
8. وضع سياسة حذف وأرشفة ونسخ احتياطي واستعادة، خاصة مع علاقات `onDelete: Cascade`.
9. إعداد Logging وError tracking وHealth checks ومراقبة الأداء.
10. تثبيت بيئات Development وStaging وProduction مع Secrets منفصلة.

### P1 — استكمال الـMVP

1. استكمال Goals: إنشاء وتعديل وحذف وتحديث Progress وربطها بالمشاريع.
2. بناء Portfolios حقيقية أو إزالة المسار من نطاق الـMVP.
3. بناء Project/Executive Dashboard بدل مسارات إعادة التوجيه.
4. استكمال Calendar وTimeline ليشملا التحرير والسحب المناسب إذا كان ذلك مطلوبًا.
5. استكمال إدارة Teams وربطها بالمشاريع.
6. استكمال Archive للإشعارات وتفضيلات الإشعار.
7. استكمال Attachments باستخدام File storage فعلي.
8. إكمال Custom Fields وTags وDependencies من الواجهة.
9. إصلاح Resource links في Project Overview.
10. توحيد النصوص والحالات الفارغة ورسائل الخطأ.

### P2 — توسعات بعد الـMVP

- Forms.
- Automation Rules.
- Project Templates.
- Time tracking المتكامل.
- Budgets.
- بوابة عميل خارجية.
- Asana API connection والمزامنة.
- Realtime collaboration.
- Mobile app أو PWA.

لا يبدأ تنفيذ P2 لمجرد وجود Models في Prisma؛ يجب اعتماد كل Feature من مالك المنتج أولًا.

## 8. متطلبات غير وظيفية

### الأمان

- التحقق من الجلسة والصلاحية في الخادم لكل قراءة وتعديل.
- عدم إرجاع `password_hash` أو `is_super_admin` ضمن User selects العامة.
- تشفير أي Token مخزن وعدم تسجيله في Logs.
- Validation صارم للمدخلات، وأحجام الملفات، وأنواعها، وعناوين URL.
- حماية من CSRF بحسب نمط NextAuth المستخدم، ومن XSS في النصوص والروابط.
- تسجيل Audit trail للعمليات الإدارية والحذف والاستيراد وتغيير الصلاحيات.

### الأداء

- Pagination أو Cursor للمهام والعملاء والإشعارات بدل تحميل مجموعات غير محدودة.
- مراجعة استعلامات Prisma وتجنب N+1.
- تحسين الصور باستخدام آلية متوافقة مع التخزين المختار.
- قياس أداء الصفحات ذات المكونات الكبيرة مثل Clients، My Tasks، Board، وTask Drawer.

### الاعتمادية والبيانات

- Migration قابلة للاختبار قبل كل Deploy.
- نسخ احتياطي تلقائي مع اختبار الاستعادة.
- معاملات Database للعمليات المركبة.
- Idempotency للاستيراد والعمليات القابلة لإعادة المحاولة.
- توحيد التعامل مع Timezone وتخزين التواريخ.

### الوصول وتجربة الاستخدام

- دعم لوحة المفاتيح وFocus management لكل Dialog وDrawer وDrag-and-drop.
- Labels وARIA ورسائل خطأ قابلة للقراءة.
- تباين ألوان مناسب.
- دعم أحجام Mobile وTablet وDesktop.
- حالات Loading وEmpty وError وPermission denied لكل رحلة.

### التوافق واللغة

الواجهة الحالية باللغة الإنجليزية أساسًا. يجب أن يقرر مالك المنتج:

- هل المنتج إنجليزي فقط أم عربي/إنجليزي؟
- هل يلزم RTL؟
- ما المنطقة الزمنية الافتراضية وصيغة التاريخ؟
- ما Terminology المعتمدة للعملاء والمشاريع والمراجعات؟

## 9. قرارات مطلوبة من مالك المنتج قبل التوسع

يجب تسجيل الإجابات في Issues أو تحديث هذه الوثيقة:

1. هل المنصة داخلية أم SaaS متاحة للتسجيل العام؟
2. ما تعريف الـMVP النهائي، وما الميزات المؤجلة صراحة؟
3. هل العميل الخارجي سيدخل المنصة أم العملاء مجرد كيانات تنظيمية؟
4. هل مطلوب دعم العربية وRTL من أول إصدار؟
5. هل مطلوب PostgreSQL، وأين ستتم الاستضافة؟
6. أين ستخزن الملفات والصور؟
7. هل Asana مطلوب للاستيراد مرة واحدة أم مزامنة مستمرة؟
8. هل إضافة العضو تتم بدعوة بريدية أم إنشاء حساب مباشر؟
9. من يحق له الاستيراد، ولماذا هو Super Admin فقط حاليًا؟
10. ما قواعد الاحتفاظ بالبيانات والحذف والنسخ الاحتياطي؟
11. ما تعريف نجاح الجودة والـSLA المقبول؟
12. ما التقارير التي يعتمد عليها القرار فعلًا؟

## 10. معايير قبول عامة

لا تعتبر أي Feature مكتملة إلا إذا:

- لها متطلبات وحالات صلاحية واضحة.
- تعمل في Happy path والحالات الفارغة والأخطاء.
- تمنع الوصول غير المصرح به على الخادم.
- لا تكسر Workspace isolation.
- لها اختبارات مناسبة للمخاطر.
- تمر عبر:

```bash
npm run skills:build
npm run lint
npm run typecheck
npm test
npm run build
```

- تعمل على Mobile وDesktop.
- لا تكشف Secrets أو بيانات خاصة في الاستجابة أو Logs.
- لها Migration وخطة Rollback عند تغيير البيانات.
- يتم تحديث هذه الوثيقة وREADME عند تغير التشغيل أو النطاق.

## 11. استراتيجية العمل المقترحة

1. إنشاء Issue لكل عنصر P0 وP1.
2. ربط كل PR بمتطلب ومعايير قبول.
3. منع الدمج إذا فشل lint أو typecheck أو tests أو build.
4. استخدام Branch قصير لكل Feature.
5. مراجعة تغييرات Prisma بعناية وإرفاق Migration.
6. عدم تعديل ملفات المستخدم أو حذف بيانات محلية أثناء الصيانة.
7. إبقاء `AGENTS.md` و`CLAUDE.md` و`.impeccable.md` و`skills/` كطبقة تعليمات دائمة.

## 12. قائمة استلام الفريق

- [ ] تشغيل المشروع محليًا من Checkout نظيف.
- [ ] مراجعة `AGENTS.md` و`README.md` وهذه الوثيقة.
- [ ] تأكيد نجاح Database setup وMigration.
- [ ] تأكيد نجاح TypeScript والاختبارات والبناء.
- [ ] إنشاء خطة لإغلاق 130 خطأ lint.
- [ ] اعتماد قرارات المنتج في القسم 9.
- [ ] تحديد قاعدة البيانات والتخزين والاستضافة للإنتاج.
- [ ] إنشاء Backlog لـP0 وP1 مع مسؤول وموعد لكل عنصر.
- [ ] إعداد Staging وبيانات اختبار غير حساسة.
- [ ] تنفيذ اختبار صلاحيات شامل قبل إطلاق أي مستخدمين حقيقيين.

## 13. مراجع داخل المشروع

- `README.md`: إعداد المشروع وأوامر التشغيل.
- `.env.example`: متغيرات البيئة المتوقعة.
- `AGENTS.md`: تعليمات العمل داخل المستودع.
- `prisma/schema.prisma`: مصدر الحقيقة الحالي لنموذج البيانات.
- `src/lib/permissions.ts`: قواعد الوصول.
- `src/actions/server-actions.ts`: عمليات العملاء والمشاريع والمهام.
- `src/actions/quality-actions.ts`: دورة مراجعة الجودة.
- `src/lib/reporting-metrics.ts`: تعريفات وحسابات التقارير.
- `src/lib/task-import.ts`: Preview وتنفيذ استيراد CSV.
- `.github/workflows/ci.yml`: بوابات CI الحالية.

